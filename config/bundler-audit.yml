# Audit all gems listed in the Gemfile for known security problems by running bin/bundler-audit.
# Advisories that do not apply here are waived by adding their id to the list below. Nothing is
# waived today; the key stays (as an empty list) because bundler-audit rejects a config file that
# does not parse to a hash.

ignore: []
